<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Cross-Origin Resource Sharing (CORS) Configuration
    |--------------------------------------------------------------------------
    |
    | هنا نقوم بتحديد القواعد التي تسمح للمتصفح بالوصول إلى الـ API الخاص بنا.
    | الإعدادات أدناه تضمن قبول طلبات الفرونت إند (Port 5500).
    |
    */

    // المسارات التي سيطبق عليها نظام الـ CORS
    'paths' => ['api/*', 'sanctum/csrf-cookie', 'login', 'logout'],

    // السماح بجميع أنواع الطلبات (GET, POST, PUT, DELETE, OPTIONS)
    'allowed_methods' => ['*'],

    // الروابط المسموح لها بالوصول (يجب أن تطابق متصفحك تماماً)
'allowed_origins' => ['*'],

    // في حال كنت تستخدم روابط ديناميكية (اتركها فارغة حالياً)
    'allowed_origins_patterns' => [],

    // الهيدرز المسموح للمتصفح إرسالها ( Authorization, Content-Type, إلخ)
    'allowed_headers' => ['*'],

    // الهيدرز التي يمكن للمتصفح قراءتها في الرد
    'exposed_headers' => [],

    // مدة تخزين إذن الـ CORS في المتصفح (بالثواني)
    'max_age' => 0,

    // تفعيل إرسال ملفات تعريف الارتباط والهيدرز الخاصة بالهوية (مهم جداً لـ Sanctum)
    'supports_credentials' => true,

];